framework:
    messenger:
        failure_transport: failed

        transports:
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                options:
                    exchange:
                        name: janus
                        type: direct
                    queues:
                        janus_flows:
                            binding_keys: [flows]
                retry_strategy:
                    max_retries: 3
                    delay: 1000
                    multiplier: 2
            failed: 'doctrine://default?queue_name=failed'
            sync: 'sync://'
            # Scheduler transport — ticks every minute to fire due cron jobs
            scheduler_magnets: 'scheduler://magnets'

        routing:
            App\Flows\Domain\Message\RunFlowMessage: async
            App\Heimdall\Domain\Message\PasswordResetEmailMessage: sync
            App\Users\Domain\Message\InviteEmailMessage: sync
            App\Portals\Domain\Message\MagnetRunMessage: async
            App\Portals\Domain\Message\ScheduledMagnetRunMessage: async

# when@test:
#    framework:
#        messenger:
#            transports:
#                # replace with your transport name here (e.g., my_transport: 'in-memory://')
#                # For more Messenger testing tools, see https://github.com/zenstruck/messenger-test
#                async: 'in-memory://'
